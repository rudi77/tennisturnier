# --- Backend ---------------------------------------------------------------
# Ziel des Dev-Proxys. Der Standard entspricht dem http-Profil aus
# src/TennisTurnier.Api/Properties/launchSettings.json.
VITE_API_PROXY_TARGET=http://localhost:5188

# Basis-URL der API. Leer lassen heißt "gleiche Herkunft" — im Dev-Betrieb also
# über den Vite-Proxy. Nur setzen, wenn das Backend getrennt ausgeliefert wird
# und CORS dort konfiguriert ist.
VITE_API_BASE_URL=

# --- Anmeldung (OIDC) ------------------------------------------------------
# Aussteller. Lokal Keycloak aus docker-compose.yml; in Produktion, was der
# Betreiber hinstellt — die Anwendung prüft nur Tokens (ADR-0007).
VITE_OIDC_AUTHORITY=http://localhost:8080/realms/tennisturnier

# Öffentlicher Client aus deploy/keycloak/import-dev/tennisturnier-realm.json —
# das ist der Realm, den docker-compose.yml einhängt.
VITE_OIDC_CLIENT_ID=tennisturnier-api

# Die angefragten Scopes. Die Audience prüft das Backend selbst
# (appsettings: Oidc:Audience); der Realm legt sie über einen Protocol Mapper
# in das Token.
VITE_OIDC_SCOPE=openid profile email
